.zig-cache/
zig-out/
zig-pkg/
*.o
.DS_Store
CLAUDE.md
.make/
.claude/worktrees/

# Only the user-facing docs ship, and the protocol lgtm-ios builds against.
# Everything else in docs/ is internal: the design documents, the roadmap, the
# working journal. They stay on disk.
docs/*.md
!docs/GUIDE.md
!docs/CONFIG.md
result

# lang samples for checking
samples
